# 📋 게임화 Todo 웹 애플리케이션 개요

## 프로젝트 비전

사용자가 **귀여운 캐릭터**를 키우면서 **습관을 형성**할 수 있는 게임화된 Todo 웹 애플리케이션입니다.
캐릭터는 사용자가 할 일을 완료할 때마다 경험치를 얻어 **성장하고 외형이 변화**합니다.

---

## 핵심 기능

### 1. 🐾 캐릭터 시스템
- **선택 화면**: 처음 접속 시 3개의 귀여운 캐릭터 중 하나를 선택
- **성장 시스템**: 경험치(XP) 기반 레벨업
- **외형 변화**: 레벨에 따른 캐릭터 외형 진화 (예: Lv1 → Lv5 → Lv10)

### 2. ✅ 습관 관리
- **습관 등록**: 제목, 내용, 난이도, 반복 요일 설정
- **난이도 선택**: 쉬움 / 보통 / 어려움 (경험치 차등 지급)
- **요일 선택**: 월~일 중 복수 선택 가능

### 3. 📅 일일 Todo 리스트
- 오늘 날짜 기준 해당 요일의 습관들만 표시
- 각 항목 완료 시 "Clear" 처리
- Clear 시 캐릭터에게 경험치 지급

---

## 기술 스택

| 영역 | 기술 |
|------|------|
| 프론트엔드 | Next.js 16, React 19, TailwindCSS 4 |
| 백엔드/DB | Supabase (PostgreSQL + Auth) |
| 아이콘 | Lucide React |
| 배포 | Vercel (예정) |

---

## 사용자 플로우

```mermaid
flowchart TD
    A[앱 접속] --> B{캐릭터 선택 완료?}
    B -->|No| C[캐릭터 선택 화면]
    C --> D[캐릭터 선택]
    D --> E[메인 대시보드]
    B -->|Yes| E
    
    E --> F[오늘의 Todo 리스트]
    E --> G[습관 관리]
    E --> H[캐릭터 상태 확인]
    
    G --> I[습관 추가/수정/삭제]
    F --> J[항목 Clear]
    J --> K[경험치 획득]
    K --> L{레벨업?}
    L -->|Yes| M[캐릭터 진화!]
    L -->|No| F
