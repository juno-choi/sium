# 📁 폴더 구조 설계

## 현재 구조 (삭제 예정 표시)

```
src/
├── app/
│   ├── (auth)/               # 유지 - 인증 레이아웃
│   ├── [userUuid]/           # 삭제 - 전단지 관련
│   ├── auth/                 # 유지 - 인증 콜백
│   ├── flyers/               # 삭제 - 전단지 페이지
│   ├── test/                 # 삭제 - 테스트 페이지
│   ├── globals.css           # 유지 - 전역 스타일
│   ├── layout.tsx            # 수정 - 레이아웃
│   └── page.tsx              # 수정 - 메인 페이지
├── components/
│   ├── flyers/               # 삭제 - 전단지 컴포넌트
│   ├── ui/                   # 유지 - UI 컴포넌트
│   ├── ErrorBoundary.tsx     # 유지
│   ├── Header.tsx            # 수정 - 헤더
│   └── ImageUpload.tsx       # 삭제 - 이미지 업로드
├── lib/
│   ├── flyer-template.ts     # 삭제 - 전단지 템플릿
│   ├── storage/              # 삭제 - 저장소
│   └── supabase/             # 유지 - Supabase 클라이언트
└── types/
    └── flyer.ts              # 삭제 - 전단지 타입
```

---

## 새로운 구조

```
src/
├── app/
│   ├── (auth)/
│   │   └── login/
│   │       └── page.tsx          # 로그인 페이지
│   ├── auth/
│   │   └── callback/
│   │       └── route.ts          # OAuth 콜백
│   ├── character-select/
│   │   └── page.tsx              # 캐릭터 선택 페이지 [NEW]
│   ├── dashboard/
│   │   └── page.tsx              # 메인 대시보드 [NEW]
│   ├── habits/
│   │   ├── page.tsx              # 습관 목록 [NEW]
│   │   ├── new/
│   │   │   └── page.tsx          # 습관 추가 [NEW]
│   │   └── [id]/
│   │       └── page.tsx          # 습관 상세/수정 [NEW]
│   ├── globals.css
│   ├── layout.tsx
│   └── page.tsx                  # 랜딩 or 리다이렉트
│
├── components/
│   ├── character/                # 캐릭터 관련 [NEW]
│   │   ├── CharacterCard.tsx     # 캐릭터 카드
│   │   ├── CharacterSelect.tsx   # 캐릭터 선택 그리드
│   │   ├── CharacterStatus.tsx   # 캐릭터 상태 표시
│   │   └── LevelProgress.tsx     # 레벨/경험치 바
│   │
│   ├── habits/                   # 습관 관련 [NEW]
│   │   ├── HabitCard.tsx         # 습관 카드
│   │   ├── HabitForm.tsx         # 습관 폼 (추가/수정)
│   │   ├── HabitList.tsx         # 습관 목록
│   │   ├── DifficultySelect.tsx  # 난이도 선택
│   │   └── DaySelect.tsx         # 요일 선택
│   │
│   ├── todo/                     # Todo 관련 [NEW]
│   │   ├── TodoItem.tsx          # Todo 항목
│   │   ├── TodoList.tsx          # 오늘의 Todo 리스트
│   │   └── ClearButton.tsx       # Clear 버튼
│   │
│   ├── ui/                       # 공통 UI 컴포넌트
│   │   ├── Button.tsx
│   │   ├── Card.tsx
│   │   ├── Input.tsx
│   │   ├── Checkbox.tsx
│   │   └── ProgressBar.tsx
│   │
│   ├── layout/                   # 레이아웃 [NEW]
│   │   ├── Header.tsx
│   │   ├── Navigation.tsx
│   │   └── Footer.tsx
│   │
│   └── ErrorBoundary.tsx
│
├── lib/
│   ├── supabase/
│   │   ├── client.ts
│   │   ├── server.ts
│   │   └── middleware.ts
│   │
│   ├── utils/                    # 유틸리티 [NEW]
│   │   ├── xp.ts                 # 경험치 계산 함수
│   │   ├── date.ts               # 날짜 유틸리티
│   │   └── constants.ts          # 상수 정의
│   │
│   └── hooks/                    # 커스텀 훅 [NEW]
│       ├── useCharacter.ts       # 캐릭터 상태
│       ├── useHabits.ts          # 습관 목록
│       └── useTodoList.ts        # 오늘의 Todo
│
├── types/                        # 타입 정의 [NEW]
│   ├── character.ts
│   ├── habit.ts
│   └── todo.ts
│
└── middleware.ts                 # 인증 미들웨어
```

---

## 주요 변경 사항

### 삭제할 파일/폴더

| 경로 | 이유 |
|------|------|
| `src/app/flyers/` | 전단지 기능 삭제 |
| `src/app/[userUuid]/` | 전단지 관련 라우트 |
| `src/app/test/` | 테스트 페이지 |
| `src/components/flyers/` | 전단지 컴포넌트 |
| `src/components/ImageUpload.tsx` | 전단지용 이미지 업로드 |
| `src/lib/flyer-template.ts` | 전단지 템플릿 |
| `src/lib/storage/` | 전단지 저장소 |
| `src/types/flyer.ts` | 전단지 타입 |

### 새로 추가할 폴더

| 폴더 | 목적 |
|------|------|
| `src/app/character-select/` | 캐릭터 선택 페이지 |
| `src/app/dashboard/` | 메인 대시보드 |
| `src/app/habits/` | 습관 관리 페이지들 |
| `src/components/character/` | 캐릭터 컴포넌트 |
| `src/components/habits/` | 습관 컴포넌트 |
| `src/components/todo/` | Todo 컴포넌트 |
| `src/lib/utils/` | 유틸리티 함수 |
| `src/lib/hooks/` | 커스텀 React 훅 |
